<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.apiserver.mapper.ServiceItemMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO service_items (name, description, price, commission_rate, status)
        VALUES (#{name}, #{description}, #{price}, #{commissionRate}, #{status})
    </insert>

    <update id="update">
        UPDATE service_items
        SET name = #{name},
            description = #{description},
            price = #{price},
            commission_rate = #{commissionRate},
            status = #{status}
        WHERE id = #{id}
    </update>

</mapper>